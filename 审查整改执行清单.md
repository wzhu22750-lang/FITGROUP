# FitGroup 项目审查整改执行清单

> 目标：修复核心数据可信度、一致性、并发和可恢复性问题。
>
> 当前结论：**C：核心设计存在问题，建议先完成本清单，再继续扩展功能。**
>
> 本文是给 AGY 的执行文档。除非另有说明，先不要做 UI 重设计，不要删除现有功能，不要引入大型新框架。

---

## 一、必须遵守的原则

1. `workoutLogs` 是训练事实源。
2. `users` 中的 `streak`、`totalWorkouts`、`prs`、`lastWorkoutDate` 都是派生数据，不能由客户端随意修改。
3. 所有写入必须考虑重复提交、网络中断、并发和 App 被杀死。
4. UI 显示成功，必须代表后端事实已经成立。
5. UI 显示失败时，必须能判断数据是否已经写入，不能让用户只能靠重复点击恢复。
6. Firestore Rules 必须验证状态转换的来源，而不只是检查最终数字是否合法。
7. 不要静默吞掉关键错误。
8. 先修复数据正确性，再做性能和体验优化。

---

## 二、P0：立即修复

### P0-1 禁止客户端伪造用户统计

**涉及文件**

- `src/api.ts`
- `src/components/WorkoutLogger.tsx`
- `src/components/Statistics.tsx`
- `firestore.rules`

**问题**

当前已登录用户可以直接修改自己的：

- `streak`
- `totalWorkouts`
- `prs`
- `lastWorkoutDate`

这会导致排行榜和个人统计不可信。

**要求**

1. Firestore Rules 禁止客户端直接修改上述派生字段。
2. 客户端只负责创建训练记录和修改个人展示资料。
3. 统计只能由可信后端逻辑根据训练记录重新计算。
4. 如果暂时没有 Cloud Functions，至少先把统计更新集中到一个受控模块，并明确标记为临时方案；不要保留多个重复计算入口。

**验收标准**

- 直接使用 Firestore SDK 修改 `streak`、`totalWorkouts`、`prs` 必须被拒绝。
- 用户新增或删除训练记录后，统计由唯一权威逻辑更新。
- 统计更新失败时，UI 不得伪装成完全成功。

---

### P0-2 修复训练记录提交的半成功和重复提交

**涉及文件**

- `src/components/WorkoutLogger.tsx`
- `src/api.ts`

**问题**

当前流程是：

```text
上传图片 → 创建训练记录 → 重新计算统计 → 更新用户资料
```

这些操作不是原子的。训练记录可能已经写入，但统计失败，UI 却显示失败。用户再次点击后会创建重复训练记录。

**要求**

1. 为每次提交增加稳定的幂等请求 ID。
2. 同一个请求 ID 重试时不能创建第二条训练记录。
3. 明确区分三种状态：
   - 已成功保存
   - 状态未知，需要查询恢复
   - 确定失败且未写入
4. 不要在 `createWorkoutLog` 和 `WorkoutLogger` 中保留两套统计更新逻辑。
5. 提交按钮在请求期间禁用，但不能只依赖按钮禁用防止重复提交。
6. 图片上传失败、训练记录失败、统计失败必须分别处理。

**验收标准**

- 快速连续点击只产生一条训练记录。
- 网络中断后重新提交不会产生重复记录。
- 训练记录已写入但统计失败时，页面明确提示“训练已保存，统计同步失败”。

---

## 三、P1：核心安全和数据一致性

### P1-1 修复评论计数器绕过

**涉及文件**

- `firestore.rules`
- `src/api.ts`

**问题**

当前规则只要求 `commentsCount` 增加 1，没有要求同时创建评论文档。任何认证用户都可能伪造评论数量。

**要求**

1. 评论文档创建和评论计数更新必须属于同一个 batch 或事务。
2. Rules 使用 `getAfter()` 验证评论文档确实被创建。
3. 客户端不能单独更新 `commentsCount`。
4. 更推荐由后端触发器维护评论计数。

**验收标准**

- 没有创建评论文档时，单独增加 `commentsCount` 必须失败。
- 创建一条评论只能让计数增加 1。
- 重试同一个评论请求不能重复计数。

---

### P1-2 阻止计数更新时注入额外字段

**涉及文件**

- `firestore.rules`

**问题**

点赞和评论计数更新规则没有严格限制字段集合，可能在计数更新时混入未声明字段。

**要求**

1. 所有写入路径都使用字段白名单。
2. 点赞计数更新只能修改点赞相关字段。
3. 评论计数更新只能修改评论相关字段。
4. 训练记录不能通过计数更新路径新增 `admin`、`isVerified` 等字段。

**验收标准**

以下字段注入必须被拒绝：

- `admin`
- `isVerified`
- `hiddenPayload`
- 任何未在 schema 中声明的字段

---

### P1-3 修复训练项目只校验前 10 项

**涉及文件**

- `firestore.rules`
- `src/types.ts`

**问题**

规则允许最多 50 个训练项目，但只验证前 10 个，后面的项目可能包含任意结构。

**要求**

二选一：

1. 如果产品最多只需要 10 个项目，把规则最大值改为 10。
2. 如果必须支持 50 个项目，把训练项目拆成子集合，或交给后端完整校验。

不要继续保持“允许 50 个但只检查 10 个”的状态。

**验收标准**

- 第 11 个及之后的项目也必须经过完整校验。
- 非法字段、非法类型、超范围数字都必须被拒绝。

---

### P1-4 补充统计查询索引并禁止静默吞错

**涉及文件**

- `src/api.ts`
- `src/components/Statistics.tsx`
- `firestore.indexes.json`

**问题**

统计查询同时使用：

- `where userId`
- `orderBy timestamp`

当前索引配置没有明确包含这两个字段的复合索引。统计失败后代码只打印警告，用户仍然可能看到旧数据。

**要求**

1. 添加 `userId + timestamp` 复合索引。
2. 部署前验证索引配置。
3. 统计同步失败时不得静默忽略。
4. UI 必须区分“训练保存成功”和“统计同步失败”。

**验收标准**

- 新用户第一次打卡后统计正确更新。
- 删除训练后统计正确减少。
- 模拟索引失败时页面明确显示统计同步失败。

---

### P1-5 删除训练记录时清理关联数据

**涉及文件**

- `src/api.ts`
- `firestore.rules`
- `storage.rules`

**问题**

删除训练记录只删除父文档，不会删除：

- likes 子集合
- comments 子集合
- Storage 中的训练图片

**要求**

1. 设计完整删除生命周期。
2. 删除父记录后清理 likes 和 comments。
3. 删除对应 Storage 图片。
4. 清理失败必须进入可重试状态。
5. 不要依赖客户端递归删除大量子数据。

**验收标准**

- 删除一条带点赞、评论、图片的训练记录后，不产生孤儿数据。
- 删除过程中断后可以继续清理。
- 删除失败时用户能看到明确状态。

---

### P1-6 统一统计计算逻辑，消除并发覆盖

**涉及文件**

- `src/api.ts`
- `src/components/WorkoutLogger.tsx`
- `src/components/Statistics.tsx`

**问题**

当前同时存在：

- 创建时增量计算
- `syncUserStatsFromLogs` 全量计算
- 用户资料实时监听

多设备同时操作时可能互相覆盖。

**要求**

1. 只保留一个统计权威逻辑。
2. 删除 `WorkoutLogger` 中重复的统计计算。
3. 统计结果必须从训练事实源推导。
4. 处理两个设备同时打卡的情况。
5. 处理打卡和删除同时发生的情况。

**验收标准**

- 两个设备同时打卡，统计不会少 1。
- 打卡和删除并发时，最终统计与实际训练记录一致。
- 旧客户端不能覆盖新统计。

---

## 四、P2：稳定性和性能

### P2-1 给动态流增加分页和数量限制

**涉及文件**

- `src/api.ts`
- `src/components/Feed.tsx`

**问题**

当前 Feed 使用全量实时订阅，没有 `limit`、分页或时间窗口。

**要求**

1. 首屏只加载最近一批记录。
2. 使用游标分页加载历史数据。
3. 限制实时监听范围。
4. 评论列表也要分页或限制数量。
5. 数据量增长后不能一次性渲染全部卡片。

**验收标准**

- 即使有大量训练记录，首屏读取数量仍然受控。
- 加载更多不会重复或丢失记录。
- Feed 不会因为历史数据增长而越来越慢。

---

### P2-2 减少每条动态单独查询点赞状态

**涉及文件**

- `src/components/Feed.tsx`
- `src/api.ts`

**问题**

每张动态卡片都会单独读取当前用户是否点赞，动态越多读取次数越多。

**要求**

- 限制动态数量。
- 批量获取点赞状态，或设计用户点赞索引。
- 操作失败后重新读取真实状态。
- 点赞请求期间禁用按钮，防止快速重复点击。

---

### P2-3 清理图片和头像资源

**涉及文件**

- `src/api.ts`
- `src/components/Profile.tsx`

**问题**

头像每次保存都会创建新文件，旧头像不删除；训练图片删除记录后也不会清理。

**要求**

- 头像使用固定路径或保存旧文件引用。
- 替换头像后删除旧文件。
- 建立未引用图片清理机制。
- 校验真实图片格式、大小和像素面积。

---

### P2-4 明确有效训练数据的最小条件

**涉及文件**

- `src/components/WorkoutLogger.tsx`
- `firestore.rules`
- `src/types.ts`

**问题**

当前允许重量、组数、次数、时长、距离和卡路里全部为 0，仍可能生成一次打卡。

**要求**

- strength 必须满足有效的重量、组数、次数组合。
- cardio 至少满足时长、距离、卡路里中的合理条件。
- UI 提供字段级错误提示。
- Rules 和客户端校验保持一致。

---

## 五、P3：整理和优化

### P3-1 清理 PocketBase 迁移残留

如果项目已经正式使用 Firebase：

- 删除 `pocketbase` 依赖。
- 删除 `src/pocketbase.ts`。
- 删除或归档 `POCKETBASE_DEPLOY.md`。
- 重写 `.env.example`。
- 更新 README。

---

### P3-2 修正文档和真实实现不一致

重点更新：

- README 中“照片上传是 stub”的描述。
- Firebase 部署说明。
- Firestore Rules 测试说明。
- Android 使用远程 URL 的行为。
- 索引部署说明。
- 数据删除和资源清理说明。

---

### P3-3 拆分生产 bundle

当前主 JS 约 1.46 MB，Vite 已发出 chunk size warning。

建议：

- 登录页动态加载。
- Statistics 动态加载 Recharts。
- SharePoster 动态加载海报依赖。
- 配置合理的 manualChunks。

---

### P3-4 建立自动化测试

至少增加：

1. Firestore Emulator Rules 测试。
2. 训练记录创建测试。
3. 重复提交测试。
4. 网络失败恢复测试。
5. 并发打卡测试。
6. 删除级联清理测试。
7. 统计重算测试。
8. 第 11 个训练项目的规则测试。
9. 评论计数绕过测试。
10. Android 真机拍照、返回键、后台恢复测试。

---

## 六、执行顺序

### 第 1 批：先修数据安全和一致性

- [ ] 禁止客户端修改派生统计
- [ ] 修复评论计数器绕过
- [ ] 限制计数更新字段
- [ ] 修复训练项目校验范围
- [ ] 补充复合索引
- [ ] 增加 Rules Emulator 测试

### 第 2 批：重写提交和统计

- [ ] 增加幂等请求 ID
- [ ] 统一统计计算逻辑
- [ ] 处理半成功和状态未知
- [ ] 处理多设备并发
- [ ] 删除重复的客户端统计代码

### 第 3 批：完善删除和资源生命周期

- [ ] 清理 likes
- [ ] 清理 comments
- [ ] 清理 Storage 图片
- [ ] 清理旧头像
- [ ] 增加失败重试和孤儿数据清理

### 第 4 批：性能和工程整理

- [ ] Feed 分页
- [ ] 评论分页
- [ ] 批量获取点赞状态
- [ ] 拆分 bundle
- [ ] 清理 PocketBase 残留
- [ ] 更新 README
- [ ] 完善自动化测试

---

## 七、完成标准

只有满足以下条件，才可以继续扩展新功能：

- [ ] 用户不能直接伪造 streak、PR、训练总数和排行榜数据。
- [ ] 重复点击不会产生重复训练记录。
- [ ] 网络中断后可以判断是否已经保存。
- [ ] 训练记录和统计不会出现长期不一致。
- [ ] 评论数不能脱离评论文档单独增加。
- [ ] 删除训练记录不会遗留不可控孤儿数据。
- [ ] 训练项目全部经过校验。
- [ ] Feed 不再全量订阅全部历史数据。
- [ ] 关键 Rules 已通过攻击场景测试。
- [ ] 真实 Android 生命周期已验证。
- [ ] 所有测试、构建和 lint 通过。

完成以上整改后，再判断是否需要进一步重构或扩展功能。
